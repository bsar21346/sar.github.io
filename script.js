// Basic script for interactive features (extendable as needed)

// Alert when navigating between pages
document.querySelectorAll('nav a').forEach(link => {
    link.addEventListener('click', (e) => {
        
    });
});
